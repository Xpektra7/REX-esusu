# Implementation Plans

Generated by the improve skill on 2026-07-09. Reconciled with `GAPS.md` on
2026-07-09 (GAPS.md was largely stale — ~19 claimed gaps already implemented;
see reconciliation notes below). Execute in the order below unless dependencies
say otherwise. Each executor: read the plan fully before starting, honor its
STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix top-up reconciliation — remove misdirected gate, eliminate double-credit | P1 | M | — | DONE |
| 002 | Secure auth — page guard proxy, BVN `code:05`, JWT hardening (+ N149, repurpose src/proxy.ts) | P1 | M | — | DONE |
| 008 | Debt visibility — payout redirection, accumulated debt, dashboard badge (+ T125/T126) | P1 | M | — | DONE |
| 003 | Harden — rate limiting, webhook `"00"`→`"09"`, OTP log cleanup | P2 | S | — | DONE |
| 004 | Fix wallet history — type mapping, withdrawal recording (+ refetch, pagination, filter) | P2 | S | — | DONE |
| 007 | Notification types + mock data completeness | P2 | M | — | DONE |
| 009 | Notification deep-linking | P2 | S | — | DONE |
| 005 | Finish Renata landing refactor — dark hero, gold curve, CTA mock | P2 | M | — | DONE |
| 010 | Mock handler completeness (non-notification) | P3 | M | — | DONE |
| 011 | PWA offline + push | P3 | M | — | DONE |
| 006 | Small hygiene — OTP schema, dead deps, dup routes, Biome (+ password/pin blacklist) | P3 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- All plans are independent — no strict execution order required. 001 is recommended first because the top-up bug blocks the core product demo.
- 002 and 003 both touch `src/proxy.ts` (002 creates it, 003 adds rate limiting inside it). If 002 is skipped, 003's proxy step must create the file instead. If both run, run 002 first.

## GAPS.md reconciliation (2026-07-09)

`GAPS.md` was a 6-persona × 154-action simulation. Verified against disk: **stale**.
Already implemented (do NOT redo): E85 CSV export, E71/T117 debt-breakdown dialog,
C53 grace countdown, C61 trust breakdown modal, N139/N140 PIN lockout, F99/F100
bankCodes/Lookup mock handlers, C57 markAllRead, D8 retry, D25 topup error,
C43 remind error, E82b bank-codes handler.

Genuinely missing → mapped as add-ons or new plans:
- **Add-ons**: 002 ← N149 + D16 BVN `code:05` + repurpose dead `src/proxy.ts` as Next proxy; 004 ← D30/F104 wallet `refetchInterval(10s)` + F91 pagination + F92 filter; 006 ← D12 password blacklist + D20/pin `0000`/sequential (QF#11/12).
- **New plans**: 007 notification types + mock data (C38, QF#6 `reminder`/`default_alert`, QF#7 `trust_score_changed`/`withdrawal_status`, C59b, T111, F104, D27, C59, C58); 008 debt visibility (T121, T120, T113, T125, T126); 009 notification deep-linking (T112/N142); 010 mock handler completeness (QF#1 `isNewUser`, QF#2 `wallet.topup`, D8b OTP rate-limit, C45 initiate balance, E77/E78 leave, E80 invite, E82 expired, F105 failed withdrawal); 011 PWA offline + push (D1, N148, N141).

## Findings considered and rejected

- **M-02 (Refresh token jti/revocation)**: Worth doing but requires DB migration for a `refresh_tokens` table and changes to the login/refresh flow. Deferred to post-MVP.
- **E-02 through E-13 (existing low-priority findings from prior audit)**: Listed in `AUDIT-FINDINGS.md`. Reviewed and none blocks the current MVP. Each is a 5-15min fix when time permits.
- **Activity feed consistency**: The activity feed is populated by database triggers/application code that we didn't audit in depth. Needs a separate audit pass.
- **Cycle closure reconciliation**: `reconcileCycle` in `reconciliation.ts` was not audited. The current scope is limited to top-up payment reconciliation only.
