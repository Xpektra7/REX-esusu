# Implementation Plans

Generated by the improve skill on 2026-07-17. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

Plans 001-009 are from the previous improve session. Plans 010-014 are new from
the current session addressing critical auth, correctness, and performance gaps.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Auth middleware — protect dashboard routes | P1 | M | — | **REWRITE** — plan was based on incorrect assumption that proxy.ts is dead code. Use Plan 010 instead. |
| 002  | Non-atomic circle activation fix | P1 | M | — | TODO |
| 003  | Env var alignment + .env.example + validation | P1 | S | — | TODO |
| 004  | Batch N+1 queries in reconciliation engine | P1 | M | 011 | Superseded by Plan 014 |
| 005  | Fix API double-prefix + Input export | P1 | S | — | TODO (verify double-prefix first — callers use `request("/circles")`) |
| 006  | Add daily frequency support | P2 | S | — | TODO |
| 007  | Enforce ₦500 default penalty | P2 | M | 011 | TODO |
| 008  | Support continuous (multi-cycle) circles | P2 | M | — | TODO |
| 009  | React hygiene improvements | P2 | S | — | TODO |
| **010** | **Harden proxy.ts auth with HttpOnly cookie + JWT verification** | **P1** | **M** | — | **TODO** |
| **011** | **Wrap all money-path writes in database transactions** | **P1** | **M** | — | **TODO** |
| **012** | **Wire up payout transfer after cycle close** | **P1** | **M** | 011 | **TODO** |
| **013** | **Fix OTP length mismatch (4 vs 6 digits)** | **P1** | **S** | — | **TODO** |
| **014** | **Batch N+1 queries in reconciliation engine** | **P1** | **M** | 011 | **TODO** |

## Recommended execution order

1. **013** — OTP fix (5-minute fix, unblocks signup/login)
2. **010** — Auth hardening (critical security, foundational for any deployment)
3. **011** — DB transactions (requires careful review but prevents money loss)
4. **012** — Payout flow (depends on 011 for transaction safety; unblocks payouts)
5. **014** — N+1 batching (depends on 011 for safe refactoring)
6. **002** — Non-atomic activation (small fix)
7. **003** — Env alignment (small config fix)
8. **005** — API path fixes (verify first then fix)
9. **006** — Daily frequency (feature addition)
10. **007** — Penalty enforcement (depends on 011)
11. **008** — Continuous circles (feature addition)
12. **009** — React hygiene (frontend cleanup)

## Dependency notes

- Plan 011 (transactions) is a prerequisite for 012 (payouts) and 014 (N+1) because those involve multi-write operations that need atomicity.
- Plan 001 is superseded by Plan 010 — do not execute Plan 001 as written.
- Plan 004 is superseded by Plan 014 — more thorough and accounts for current codebase state.

## Findings considered and rejected

- Plan 001: Superseded by Plan 010. The original plan described proxy.ts as "dead code" which was incorrect — proxy.ts is the active middleware in Next.js 16.
- Plan 004: Superseded by Plan 014. The original plan covered N+1 issues but was written against an earlier codebase state.
- Distributed rate limiting (Upstash Redis): Worth doing but not critical for MVP. The in-memory rate limiter provides basic protection during development.
- CORS handling: Added to Plan 010 scope (proxy.ts changes). Separate plan not needed.
- Security headers: Added to Plan 010 scope (proxy.ts changes). Separate plan not needed.
